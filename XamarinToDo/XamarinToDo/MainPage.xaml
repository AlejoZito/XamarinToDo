<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="XamarinToDo.MainPage">
  <ContentPage.Content>
    <StackLayout>
        <Label Text="To do List" VerticalOptions="Center" HorizontalOptions="Center" />
        <Entry x:Name="newItem" Placeholder="Nuevo item" />
        <Button x:Name="addItem" Text="+" Clicked="AddItem" />
        <ListView x:Name="toDoList">
          <ListView.ItemTemplate>
            <DataTemplate>
              <ViewCell>
                <ViewCell.View>
                  <StackLayout Orientation="Horizontal">
                      <Label Text="{Binding Text}" HorizontalOptions="FillAndExpand" />
                      <Button x:Name="removeItem" CommandParameter="{Binding Text}" Text="X" Clicked="RemoveItem"/>
                  </StackLayout>
                </ViewCell.View>
              </ViewCell>
            </DataTemplate>
          </ListView.ItemTemplate>
        </ListView>
    </StackLayout>
  </ContentPage.Content>
</ContentPage>